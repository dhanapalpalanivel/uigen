Update any Vulerable dependendcies

DO the following:
1. Run `npm audit` to find the Vulerable dependendcies
2. Run `npm audit fix` to apply the updates
3. Run the test and verify the updates did not break anything